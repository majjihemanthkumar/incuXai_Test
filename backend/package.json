{
    "name": "incuxai-backend",
    "version": "1.0.0",
    "description": "Cloudflare Worker and Durable Objects for INCUXAI Live Exam",
    "main": "src/index.ts",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "db:init": "wrangler d1 execute incuxai-db --local --file=./schema.sql",
        "db:init:remote": "wrangler d1 execute incuxai-db --file=./schema.sql"
    },
    "dependencies": {
        "hono": "^4.4.7"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240725.0",
        "typescript": "^5.5.4",
        "wrangler": "^3.67.1"
    }
}